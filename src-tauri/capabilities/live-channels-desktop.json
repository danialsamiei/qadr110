{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "live-channels-desktop",
  "description": "Desktop plugins required by the QADR110 live channels window",
  "windows": ["live-channels"],
  "permissions": [
    "store:default",
    "window-state:default"
  ]
}
